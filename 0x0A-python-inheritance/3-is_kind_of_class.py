#!/usr/bin/python3
"""checks to see if an object is instance of a class"""


def is_kind_of_class(obj, a_class):
    """checks to see if an object is instance of a class"""

    return isinstance(obj, a_class)
